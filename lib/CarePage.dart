import 'package:flutter/material.dart';
class CarePage extends StatelessWidget{
  Widget build(BuildContext context)
  {
    return Scaffold(
      appBar: AppBar(
        title: Text("Care Takers"),
      ),
    );
  }

}